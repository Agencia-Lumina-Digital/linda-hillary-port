import { Box, Typography } from '@mui/material';

export const CheckoutBemolContent = () => {
  return (
    <Box>
      <Typography variant="h6">Conteúdo em construção...</Typography>
    </Box>
  );
};
